version: 1

# Public project manifest (env.oci.yaml) — canonical for ELR.
# - sops.sync: which age key this repo uses (catalog lives in ~/.config/elr/config.yaml)
# - imports: optional legacy env injection (omit when using .env.sops + direnv only)

sops:
  sync:
    key: default
    env_file: .env.sops

# imports:
#   - provider: oci
#     location: dev-env
#     vars:
#       - GH_TOKEN

# local:
#   CLI_PROXY_BASE_URL: https://proxy.example.com/v1
