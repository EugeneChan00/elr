version: 1

# Local SOPS demo — no Oracle sync required when keys.txt already exists.
sops:
  keys_file: ./keys.txt
  keys: {}   # local demo — no OCI sync; keys.txt written by setup.sh
  env:
    app: demo.env.sops

local:
  DEMO_MODE: "1"
