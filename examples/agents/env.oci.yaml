# Copy to ~/.agents/env.oci.yaml
# Secrets (including the proxy URL) come from OCI; this file only names them.
version: 1

imports:
  - provider: oci
    location: dev-env
    vars:
      - CLI_PROXY_URL
      - CLI_PROXY_API_KEY
      - OPENAI_API_KEY

local: {}
